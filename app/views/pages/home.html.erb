<main class="container">
  <h1 class="text-3xl font-bold underline"> 
    A Hashrocket Project 
  </h1>

  <% if user_signed_in? %>
    <%= link_to "All Posts", posts_path, class: "bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded-full" %>
  <% else %>
    <%= link_to "Sign in", new_user_session_path, class: "bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded-full" %>
  <% end %>
</main>