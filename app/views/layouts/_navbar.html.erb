<div class="navbar">
  <div class="app_title">
    <h1 class="font-bold text-4xl text-blue-500"> Jack's Blog </h1>
  </div>
  <div class= "actions">
    <% if user_signed_in? %>
      <%= button_to "logout", destroy_user_session_path, method: :delete, class: "bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded-full" %>
      <%if is_admin? %>
        <% button_to "Create Users", registration_path, class: "bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded-full" %>
      <% else %>
      <% end %>
      <% else %>
    <% end %>
  </div>
</div>
