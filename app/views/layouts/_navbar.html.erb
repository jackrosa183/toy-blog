<div class="navbar">
  <% if user_signed_in? %>
    <%= button_to "logout", destroy_user_session_path, method: :delete %>
  <% else %>
  <% end %>
  <h1 class="font-bold text-4xl text-blue-500"> Jack's Blog </h1>
</div>