<div class="navbar flex p-2 pt-4 ml-2">
    <h1 class="font-bold text-6xl text-blue-500 ml-4 pt-2"> Jack's Blog </h1>
  <div class= "navbar_actions">
    <% if user_signed_in? %>

      <div class="navbar_logout pt-5">
        <%= button_to "logout", destroy_user_session_path, method: :delete, class: "bg-red-500 hover:bg-red-700 text-white text-xs font-bold py-1 px-2 rounded-full" %>
      </div>

      <%if is_admin? %>
        <div class="navbar_users pt-5">
          <%= button_to "Users", users_path, method: :get, class: "bg-blue-500 hover:bg-blue-700 text-white text- xsfont-bold py-1 px-2 rounded-full" %>
        </div>
        
      <% else %>
      <% end %>
    <% else %>
    <% end %>
  </div>
</div>
