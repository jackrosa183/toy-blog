<main class="container">
  <div class= "user">
    <div class="user__email #{user.id}">
      <p>Email:<p>
      <%= tag.p user.email %>
    </div> 
    <%= button_to "Delete", user, method: :delete, class: "bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded-full" %>
  </div>
</main>