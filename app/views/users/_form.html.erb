<%= simple_form_for @user do |f| %>
  <%= f.input :email %>
  <%= f.input :password %>
  <%= f.button :submit, class: "bg-green-500 hover:bg-green-300 text-white font-bold py-2 px-8 rounded-full"%>
<% end %>