<main class="container pl-10">
  <div class="flex space-x-4 items-center justify-center h-100">
    <%= turbo_frame_tag "turbo_new_post" do %>
      <%= link_to "Create Post",
                  new_post_path,
                  class: "btn-primary",
                  data: { turbo_frame: "turbo_new_post" } %>
    <% end %>
      <%= link_to "View drafts",
                  "/index_drafts",
                  class: "btn-primary" %>
  <%= will_paginate @posts %>
  </div>

  <%= tag.p @posts.count.to_s + " Posts found", class: "text-center pt-4" %>
  <%= render @posts %>

  <%= will_paginate @posts %>

  <div class="admin_message">
    <% if current_user.admin == true %>
      <p class="italic">Welcome admin</p>
    <% end %>
  </div>
</main> 
